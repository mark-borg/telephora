[project]
name = "msc-repo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mark Borg", email = "mark.borg@vgw.co" }
]
requires-python = ">=3.10"
dependencies = ["cryptography"]

[project.scripts]
msc-repo = "msc_repo:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
